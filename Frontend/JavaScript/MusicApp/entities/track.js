export function topTracks(params) {
    this.name = params.name;
    this.album = params.album;
    this.image = params.image;
    // this.artistName = params.artistName;
    this.artist = params.artist;
}